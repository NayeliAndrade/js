/* es una instriccion que le da al navegador para que le diga como se va a ejecutar el codigo */

/* no permite parametros duplicados */
function saludar(nombre,apellido,nombre){
    console.log(`hola ${nombre} ${apellido}`);
}